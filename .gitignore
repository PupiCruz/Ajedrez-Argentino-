# Backups manuales (git ya cumple esta función)
*.bak
*.bak-*

# Archivos temporales de trabajo
*.tmp

# Configuración local de herramientas / preview
.claude/

# Etapa 2: split de datos generado (derivado de data/embedded-data.js; lo regenera el export).
# El dev usa embedded-data.js (modo eager); el split es solo para el build publicado / pruebas ?ondemand.
data/manifest.js
data/t/
data/p/

# Sistema operativo
Thumbs.db
desktop.ini
.DS_Store
